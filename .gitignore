# Local .terraform directories
.terraform/

# SSH keys (privadas y públicas) - no versionar
./keys/*.key
./keys/*.pem
./keys/*.ppk
./keys/*.pub




# .tfstate files
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data specific to the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local testing or development.
override.tf
override.tf.json
*.override.tf
*.override.tf.json
